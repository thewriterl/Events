version: '2'
services:
  events-postgresql:
    image: postgres:11.5
    # volumes:
    #     - ~/volumes/jhipster/Events/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Events
      - POSTGRES_PASSWORD=
    ports:
      - 5432:5432
